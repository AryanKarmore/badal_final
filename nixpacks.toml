[phases.setup]
nixPkgs = ["python312", "pip"]

[phases.install]
cmds = [
    "pip install --upgrade pip setuptools wheel",
    "pip install -r requirements.txt"
]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT app:app --timeout 300 --workers 1 --max-requests 1000"
